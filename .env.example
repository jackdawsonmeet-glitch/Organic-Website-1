# Preferred public origin for canonical links, sitemap URLs, and site-name markup.
# On Netlify, the built-in production URL is used when SITE_URL is unset.
# Set this to your verified domain when deploying elsewhere or using a custom domain.
SITE_URL=

# Optional HTML-tag verification token from Google Search Console (not the whole tag).
GOOGLE_SITE_VERIFICATION=
